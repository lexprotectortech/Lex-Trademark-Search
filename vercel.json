{
  "version": 2,
  "builds": [
    { "src": "client/build", "use": "@vercel/react" }, // Or @vercel/next if using Next.js
    { "src": "server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "server.js" },
    { "src": "/(.*)", "dest": "client/build" }
  ]
}
